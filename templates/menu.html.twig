<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active d-flex align-items-center gap-2" href="{{ path('app_accueil') }}">
          <img src="{{ asset('img/home-icon.png') }}" alt="Accueil" width="30" height="30">
          <span class="visually-hidden">(current)</span>
          </a>
        </li>
        {% if not app.user %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_user_new') }}">Inscription
          </a>
        </li>
        {% endif %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
          <img src="{{ asset('img/user-icon.png') }}" alt="Inscription" width="30" height="30">
          </a>
          <div class="dropdown-menu">
            {% if app.user %}
            <a class="dropdown-item" href="{{ path('app_user_profile') }}">Mon Profil</a>
            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
            <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
          </div>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_catalogues') }}">
          <img src="{{ asset('img/catalog-icon.png') }}" alt="Catalogue" width="30" height="30">
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_cart_cart_index') }}">
          <img src="{{ asset('img/cart-icon.png') }}" alt="Panier" width="30" height="30">
          </a>
        </li>
      </ul>
      <form class="d-flex" method="GET" action="{{ path('app_search') }}">
        <input class="form-control me-sm-2" type="search" name="q" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>